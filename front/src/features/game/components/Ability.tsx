import React from 'react';
import styled from 'styled-components';
import { useNavigate } from 'react-router-dom';

const Wrapper = styled.div`

`;

const Ability = ({attendance}:any) => {
  const navigate = useNavigate();

  return (
    <Wrapper>
      
    </Wrapper>
  );
};

export default Ability;
